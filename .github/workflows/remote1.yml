name: Remote1 CI

on:
  push:
    branches: [master]
    paths:
      - 'remote1/**'
  pull_request:
    branches: [master]
    paths:
      - 'remote1/**'

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: remote1
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install
      - run: npm run build
